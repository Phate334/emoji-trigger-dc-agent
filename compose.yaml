services:
  bot:
    build: .
    image: emoji-trigger-agent:latest
    environment:
      DISCORD_BOT_TOKEN: ${DISCORD_BOT_TOKEN:-}
    volumes:
      - ./codex/runtime:/app/codex/runtime
    restart: unless-stopped
